Based on everything you've shown, these are the **last improvements I'd make before deployment**.

## 1. Add a Pre-Launch Reset Script (Highest Priority)

Tell Replit:

Create a production launch reset script.

File:
scripts/prelaunch-reset.ts

Requirements:

KEEP:

* users
* students
* batches
* chapters
* scheduled tests
* academy settings

DELETE:

* student test results
* subject marks
* rank history
* attendance history
* notifications
* whatsapp drafts
* demo analytics
* demo rankings
* demo test records

After reset:

* all students remain
* all teacher accounts remain
* all batches remain
* database is ready for the first real weekly test

Add a safety confirmation flag so it cannot run accidentally.

---

## 2. First Login Password Change

Current:

```txt
student@123
```

for everyone.

Before deployment:

Add:

```txt
mustChangePassword=true
```

Then:

```txt
Login
↓
Force Change Password
↓
Dashboard
```

This is a major security improvement.

---

## 3. Remove Demo Branding

Check for:

```txt
Demo Student
Demo Account
Demo Credentials
Quick Login
```

Keep only:

```txt
Teacher Login
Student Login
```

on production.

---

## 4. Add Database Backup

Tell Replit:

Create backup and restore scripts.

scripts/backup-db.ts
scripts/restore-db.ts

Neon PostgreSQL compatible.

Allow full export and import before upgrades.

---

## 5. Add Admin Emergency Account

Create one hidden emergency teacher account:

```txt
Role: SUPER_ADMIN
```

Only for recovery.

Do not expose in UI.

---

## 6. Add Launch Mode

In `.env`

```env
APP_MODE=production
```

Behavior:

Production:

* No demo banners
* No demo credentials
* No seed buttons

Development:

* Demo tools visible

---

## 7. WhatsApp Improvements

You already have:

```txt
19 drafts queued
19 messages sent
```

Good.

Add:

```txt
Resend Failed
Send Batch
Send All
```

and export logs.

---

## 8. Keep Website Lightweight

Replit already improved:

```txt
846 kB → 228 kB
```

That's a huge win.

I would stop optimizing now unless Lighthouse drops below 85.

---

## 9. Production Launch Data

For launch day:

### Keep

```txt
Students
Teachers
Batches
Chapters
Scheduled Tests
```

### Remove

```txt
Fake Scores
Fake Attendance
Fake Notifications
Fake Rankings
Fake WhatsApp Drafts
```

---

## 10. Final Launch Checklist

Before I would approve deployment:

```txt
✓ Build passes
✓ Health endpoint works
✓ Upload marks works
✓ Rank generation works
✓ Leaderboard updates
✓ Student dashboard updates
✓ WhatsApp send all works
✓ Password change works
✓ Database backup exists
✓ Launch reset script exists
✓ Demo data removed
✓ GitHub updated
✓ Neon connected
✓ Railway connected
✓ Vercel connected
```

---

### My Overall Recommendation

Do **not** spend more tokens on new features.

Do:

1. Launch reset script
2. Force password change
3. Remove demo artifacts
4. Backup script
5. Final deployment

Everything else can be Phase 2 after real students start using it.

**Deployment readiness:** ~95/100 if the reported tests are accurate. The remaining risk is mostly operational (demo cleanup, backups, and first-login security), not missing features.
