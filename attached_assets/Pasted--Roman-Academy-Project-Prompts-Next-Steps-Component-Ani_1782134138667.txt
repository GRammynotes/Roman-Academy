# Roman Academy Project Prompts & Next Steps

## Component & Animation Mapping  
- **GradientText** – Hero title text animation (e.g. “ROMAN ACADEMY 11TH • 12TH • CET”).  
- **ShapeGrid** – Hero section animated background (navy and gold shapes).  
- **ScrollFloat** – Hero tagline (words like “PERSONAL TUITION / CONCEPT FOCUSED / RESULT DRIVEN”) *and* small text headings/subtitles.  
- **StarBorder** – CTA button hover effect (e.g. **Enquire Now**, **View Batches**).  
- **PixelCard** – Faculty profile cards with hover glow.  
- **GridScan** – Background effect in Active Batches section.  
- **Noise** – Subtle noise texture for the Contact page background.  
- **Ferrofluid** – Animated background for the Login page behind the form.  
- **CardNav / Dock** – Navbar animations (Dock magnification on desktop menu; CardNav style for mobile hamburger).  
- **AnimatedList** – Dynamic lists (e.g. student lists, leaderboards with count-up stats).  

*(Copy sequence for implementation: GradientText, ShapeGrid, ScrollFloat, StarBorder, PixelCard, GridScan, Noise, Ferrofluid, CardNav, AnimatedList.)*

## Global Theme & Design  
- **Colors:** Royal Navy Blue (#071A3D), Deep Midnight Blue (#020B1F), Premium Gold (#D4AF37), Light Gold (#F3D27A), White.  
- **Typography & Style:** Use the Roman Academy brochure’s gold metallic lettering style. Employ thin gold border outlines, dark navy backgrounds, and minimalistic, “premium” academy styling. No neon or glassmorphism.  
- **Animations:** Only use one subtle animation per component. Avoid cluttering pages with simultaneous animations.  
- **Dark/Light Toggle:** Implement a theme toggle that swaps between light/dark modes, but ensure both themes adhere to the gold/navy color scheme and brochure aesthetics.  

## Page & Feature Breakdown

- **Landing Page (Hero Section):**  
  - **Layout:** Large hero section. Left-aligned title “ROMAN ACADEMY” in gold gradient text (use *GradientText*). Decorate with a thin gold outline frame influenced by the brochure. Hero background animated with *ShapeGrid* (low opacity navy + gold shapes on the right).  
  - **Buttons:** Two CTAs (“Enquire Now”, “View Batches”) with gold star-border animation (use *StarBorder*).  
  - **Tagline:** Text (“PERSONAL TUITION • CONCEPT FOCUSED • RESULT DRIVEN”) that floats in on scroll (use *ScrollFloat*).  

- **Navbar:**  
  - **Desktop:** Use a floating dark navy navbar with thin gold border, Roman Academy logo on the left. Menu items (Home, Batches, Results, etc.) use a magnifying *Dock* animation on hover. The navbar is sticky at top.  
  - **Mobile:** Use a hamburger menu styled with *CardNav* animations.  

- **Active Batches Section (Courses):**  
  - **Background:** Subtle *GridScan* animation in the background.  
  - **Batch Cards:** Show active batch cards. On mobile, paginate one card at a time with “Next” and “Previous” buttons.  

- **Results Section:**  
  - Keep a professional, clean layout.  
  - Only include animated count-up statistics for: Top Score, Students Above 80%, Toppers, Pass %. (No fancy animations beyond count-up.)  

- **Faculty Section:**  
  - Display faculty profiles using *PixelCard*.  
  - **Faculty Card Style:** Dark luxury card with gold accents. On hover, the card has a pixelated effect and the teacher’s name text glows slightly (remain readable).  

- **Gallery Section:**  
  - Use *Scroll Stack* layout for images (responsive stack on mobile, multi-row desktop gallery).  

- **Contact Page:**  
  - **Background:** Add a very subtle *Noise* texture over a dark navy background.  
  - **Layout:** Two-column on desktop, single column on mobile. Use gold icons and brochure-styled fonts. No heavy animations.  

- **Login Page:**  
  - **Background:** *Ferrofluid* animation behind the login form (subtle, so the form remains focal).  
  - **Form:** Dark navy with gold form borders. Premium look and feel.  

- **Student Management (Teacher View):**  
  - Use *AnimatedList* for lists of students, attendance, results, batch rosters, etc.  
  - **Editing:** Teachers should have an “Edit” option on each student row to update profile (including resetting password) and auto-sync changes.  

- **Dashboards (Student/Teacher):**  
  - **Student Dashboard:** Keep existing design; only apply subtle visual polish (colors, spacing, fonts consistent with theme).  
  - **Teacher Dashboard:** Same—maintain existing layout, use animations only sparingly (e.g., count-ups, list fades) to match the new UI theme.  

- **Do Not Use:** Glassmorphism, neon colors, animated forms/tables/charts, repeating brochure images, or multiple effects on a single component.  

## Feature Implementation Checklist

- **Remove Streams:** Eliminate any references to NEET or Commerce streams across the site (leaderboards, filters, forms, etc.). No NEET/Commerce fields should remain.  
- **Teacher Magic Login:** Implement a special teacher login shortcut. Provide a link/token that directly logs in a teacher (bypassing repeated login) but still maintain a secure login process. Maintain default demo credentials in the system for backup (Teacher: `roman_sir` / `Roman@123`, Demo Student: `kunal.datkhile.2026` / `student@123`), but do not display these hints on the UI except for a hidden “Teacher Login?” link if needed.  
- **Credential Prompts:** Remove printed demo credentials from the login page. The only option on login should be **Teacher Login** and **Student Login**. No generic “Demo” accounts listed publicly.  
- **Password Security:** On first login (for all seeded/demo accounts), enforce `mustChangePassword=true` so users are prompted to change their password.  
- **Data Editing (Teachers):** In the teacher’s student management interface, allow teachers to edit student profiles (name, username, phone, batch, etc.) and reset student passwords. Changes should auto-sync with the database. Ensure students cannot see other students’ profiles or make edits to their own records.  
- **Student Search:** Enable searching/filtering students in teacher view by multiple fields (name, username, phone number, batch). Use the best matching field(s) for the UI (e.g., a search bar or filters).  
- **WhatsApp Access:** Restrict any WhatsApp number or message features to teacher roles only. If there is a WhatsApp messaging interface, it should appear only for teachers (e.g., in the teacher dashboard or student management).  
- **UI/UX Polish:** Ensure all fonts, colors, and spacing match the Roman Academy branding (using the provided Roman Academy brochure design as the source of truth). Use bright grid/background patterns inspired by the brochure on the landing page. Confirm the dark/light toggle works seamlessly with the gold/navy theme.  
- **React-Bits Animations:** Verify that each listed React-Bits component is correctly integrated where specified (listed under Component Mapping).  
- **Pre-Launch Scripts:** (Non-UI tasks) Create the following scripts for deployment readiness:  
  - **scripts/prelaunch-reset.ts:** Clears demo data (test scores, logs, attendance, notifications, etc.) while preserving users, batches, chapters, settings. Include a confirmation flag.  
  - **scripts/backup-db.ts** and **scripts/restore-db.ts:** Full database export/import capability (compatible with Neon PostgreSQL).  
  - **Emergency Account:** Seed one hidden SUPER_ADMIN teacher account for recovery (not visible in UI).  
- **Environment Settings:** Add `APP_MODE=production` in `.env`. Ensure production mode hides any demo banners/seed tools, while development mode can show them.  
- **Final Deployment Checklist:** Before launch, confirm: build passes; health endpoints work; marks upload and rank generation function; leaderboards and dashboards update correctly; WhatsApp “send all” works; password change flow triggers; backup and reset scripts work; demo data is removed; repository is up-to-date; database and hosting (Neon, Railway, Vercel) are connected.  

## Checkpoints for Next Agent  
- **Streams Removed:** Ensure *all* NEET/Commerce references are gone from code and UI.  
- **Teacher Login:** Test the special teacher login shortcut or hint link. Verify teachers can log in directly and still access password-change flow.  
- **Demo Accounts:** Verify only the allowed demo credentials exist; confirm login page no longer displays them publicly.  
- **Student Editing:** Confirm teachers can edit student info (including password) from the management list, and these changes take effect immediately. Students should have no such edit options.  
- **Search Functionality:** Test student search by name, username, phone, and batch—ensure relevant results.  
- **UI Consistency:** Check that colors, fonts, spacing, and animations match the Roman Academy brochure style. Test dark/light mode toggle and ensure theme consistency.  
- **Animations:** Verify each React-Bits animation is present in its designated section and functioning smoothly without performance issues.  
- **WhatsApp Visibility:** Log in as teacher vs student and check that any WhatsApp messaging options appear only for teachers.  
- **Security Settings:** Confirm the `mustChangePassword` flag is enforced.  
- **Scripts:** Run the prelaunch-reset script (in a safe environment) to ensure it deletes the intended data and preserves critical records. Test backup and restore scripts.  
- **Final QA:** Walk through the final checklist tasks above to catch any missed items before deployment.  

**Confidence:** 0.90  
**Caveat:** This plan is compiled directly from provided prompts. Please verify against the codebase that each item is feasible and that all requirements are fully captured.